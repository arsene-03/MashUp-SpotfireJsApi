<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pnd.mashup.Library.mapper.SpotfireMapper">
	
	<!-- 전체 회원정보 조회 -->
	<select id="selectLibrary" resultType="Lib_items">
		SELECT title FROM Lib_items
	</select>

</mapper>